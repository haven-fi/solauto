/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Option,
  OptionOrNullable,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  array,
  bool,
  mapSerializer,
  option,
  struct,
  u64,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type LendingAccountRepayInstructionAccounts = {
  marginfiGroup: PublicKey | Pda;
  marginfiAccount: PublicKey | Pda;
  signer: Signer;
  bank: PublicKey | Pda;
  signerTokenAccount: PublicKey | Pda;
  bankLiquidityVault: PublicKey | Pda;
  tokenProgram?: PublicKey | Pda;
};

// Data.
export type LendingAccountRepayInstructionData = {
  discriminator: Array<number>;
  amount: bigint;
  repayAll: Option<boolean>;
};

export type LendingAccountRepayInstructionDataArgs = {
  amount: number | bigint;
  repayAll: OptionOrNullable<boolean>;
};

export function getLendingAccountRepayInstructionDataSerializer(): Serializer<
  LendingAccountRepayInstructionDataArgs,
  LendingAccountRepayInstructionData
> {
  return mapSerializer<
    LendingAccountRepayInstructionDataArgs,
    any,
    LendingAccountRepayInstructionData
  >(
    struct<LendingAccountRepayInstructionData>(
      [
        ['discriminator', array(u8(), { size: 8 })],
        ['amount', u64()],
        ['repayAll', option(bool())],
      ],
      { description: 'LendingAccountRepayInstructionData' }
    ),
    (value) => ({
      ...value,
      discriminator: [79, 209, 172, 177, 222, 51, 173, 151],
    })
  ) as Serializer<
    LendingAccountRepayInstructionDataArgs,
    LendingAccountRepayInstructionData
  >;
}

// Args.
export type LendingAccountRepayInstructionArgs =
  LendingAccountRepayInstructionDataArgs;

// Instruction.
export function lendingAccountRepay(
  context: Pick<Context, 'programs'>,
  input: LendingAccountRepayInstructionAccounts &
    LendingAccountRepayInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'marginfi',
    'MFv2hWf31Z9kbCa1snEPYctwafyhdvnV7FZnsebVacA'
  );

  // Accounts.
  const resolvedAccounts = {
    marginfiGroup: {
      index: 0,
      isWritable: false as boolean,
      value: input.marginfiGroup ?? null,
    },
    marginfiAccount: {
      index: 1,
      isWritable: true as boolean,
      value: input.marginfiAccount ?? null,
    },
    signer: {
      index: 2,
      isWritable: false as boolean,
      value: input.signer ?? null,
    },
    bank: { index: 3, isWritable: true as boolean, value: input.bank ?? null },
    signerTokenAccount: {
      index: 4,
      isWritable: true as boolean,
      value: input.signerTokenAccount ?? null,
    },
    bankLiquidityVault: {
      index: 5,
      isWritable: true as boolean,
      value: input.bankLiquidityVault ?? null,
    },
    tokenProgram: {
      index: 6,
      isWritable: false as boolean,
      value: input.tokenProgram ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: LendingAccountRepayInstructionArgs = { ...input };

  // Default values.
  if (!resolvedAccounts.tokenProgram.value) {
    resolvedAccounts.tokenProgram.value = context.programs.getPublicKey(
      'splToken',
      'TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA'
    );
    resolvedAccounts.tokenProgram.isWritable = false;
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getLendingAccountRepayInstructionDataSerializer().serialize(
    resolvedArgs as LendingAccountRepayInstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
