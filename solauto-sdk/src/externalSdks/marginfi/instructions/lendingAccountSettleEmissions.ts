/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  array,
  mapSerializer,
  struct,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type LendingAccountSettleEmissionsInstructionAccounts = {
  marginfiAccount: PublicKey | Pda;
  bank: PublicKey | Pda;
};

// Data.
export type LendingAccountSettleEmissionsInstructionData = {
  discriminator: Array<number>;
};

export type LendingAccountSettleEmissionsInstructionDataArgs = {};

export function getLendingAccountSettleEmissionsInstructionDataSerializer(): Serializer<
  LendingAccountSettleEmissionsInstructionDataArgs,
  LendingAccountSettleEmissionsInstructionData
> {
  return mapSerializer<
    LendingAccountSettleEmissionsInstructionDataArgs,
    any,
    LendingAccountSettleEmissionsInstructionData
  >(
    struct<LendingAccountSettleEmissionsInstructionData>(
      [['discriminator', array(u8(), { size: 8 })]],
      { description: 'LendingAccountSettleEmissionsInstructionData' }
    ),
    (value) => ({
      ...value,
      discriminator: [161, 58, 136, 174, 242, 223, 156, 176],
    })
  ) as Serializer<
    LendingAccountSettleEmissionsInstructionDataArgs,
    LendingAccountSettleEmissionsInstructionData
  >;
}

// Instruction.
export function lendingAccountSettleEmissions(
  context: Pick<Context, 'programs'>,
  input: LendingAccountSettleEmissionsInstructionAccounts
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'marginfi',
    'MFv2hWf31Z9kbCa1snEPYctwafyhdvnV7FZnsebVacA'
  );

  // Accounts.
  const resolvedAccounts = {
    marginfiAccount: {
      index: 0,
      isWritable: true as boolean,
      value: input.marginfiAccount ?? null,
    },
    bank: { index: 1, isWritable: true as boolean, value: input.bank ?? null },
  } satisfies ResolvedAccountsWithIndices;

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data =
    getLendingAccountSettleEmissionsInstructionDataSerializer().serialize({});

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
