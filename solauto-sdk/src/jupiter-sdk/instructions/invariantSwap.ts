/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  array,
  mapSerializer,
  struct,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type InvariantSwapInstructionAccounts = {
  swapProgram: PublicKey | Pda;
  state: PublicKey | Pda;
  pool: PublicKey | Pda;
  tickmap: PublicKey | Pda;
  accountX: PublicKey | Pda;
  accountY: PublicKey | Pda;
  reserveX: PublicKey | Pda;
  reserveY: PublicKey | Pda;
  owner: PublicKey | Pda;
  programAuthority: PublicKey | Pda;
  tokenProgram?: PublicKey | Pda;
};

// Data.
export type InvariantSwapInstructionData = { discriminator: Array<number> };

export type InvariantSwapInstructionDataArgs = {};

export function getInvariantSwapInstructionDataSerializer(): Serializer<
  InvariantSwapInstructionDataArgs,
  InvariantSwapInstructionData
> {
  return mapSerializer<
    InvariantSwapInstructionDataArgs,
    any,
    InvariantSwapInstructionData
  >(
    struct<InvariantSwapInstructionData>(
      [['discriminator', array(u8(), { size: 8 })]],
      { description: 'InvariantSwapInstructionData' }
    ),
    (value) => ({
      ...value,
      discriminator: [187, 193, 40, 121, 47, 73, 144, 177],
    })
  ) as Serializer<
    InvariantSwapInstructionDataArgs,
    InvariantSwapInstructionData
  >;
}

// Instruction.
export function invariantSwap(
  context: Pick<Context, 'programs'>,
  input: InvariantSwapInstructionAccounts
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'jupiter',
    'JUP6LkbZbjS1jKKwapdHNy74zcZ3tLUZoi5QNyVTaV4'
  );

  // Accounts.
  const resolvedAccounts = {
    swapProgram: {
      index: 0,
      isWritable: false as boolean,
      value: input.swapProgram ?? null,
    },
    state: {
      index: 1,
      isWritable: false as boolean,
      value: input.state ?? null,
    },
    pool: { index: 2, isWritable: true as boolean, value: input.pool ?? null },
    tickmap: {
      index: 3,
      isWritable: true as boolean,
      value: input.tickmap ?? null,
    },
    accountX: {
      index: 4,
      isWritable: true as boolean,
      value: input.accountX ?? null,
    },
    accountY: {
      index: 5,
      isWritable: true as boolean,
      value: input.accountY ?? null,
    },
    reserveX: {
      index: 6,
      isWritable: true as boolean,
      value: input.reserveX ?? null,
    },
    reserveY: {
      index: 7,
      isWritable: true as boolean,
      value: input.reserveY ?? null,
    },
    owner: {
      index: 8,
      isWritable: false as boolean,
      value: input.owner ?? null,
    },
    programAuthority: {
      index: 9,
      isWritable: false as boolean,
      value: input.programAuthority ?? null,
    },
    tokenProgram: {
      index: 10,
      isWritable: false as boolean,
      value: input.tokenProgram ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Default values.
  if (!resolvedAccounts.tokenProgram.value) {
    resolvedAccounts.tokenProgram.value = context.programs.getPublicKey(
      'splToken',
      'TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA'
    );
    resolvedAccounts.tokenProgram.isWritable = false;
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getInvariantSwapInstructionDataSerializer().serialize({});

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
