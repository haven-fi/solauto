/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  array,
  mapSerializer,
  struct,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type MercurialSwapInstructionAccounts = {
  swapProgram: PublicKey | Pda;
  swapState: PublicKey | Pda;
  tokenProgram?: PublicKey | Pda;
  poolAuthority: PublicKey | Pda;
  userTransferAuthority: PublicKey | Pda;
  sourceTokenAccount: PublicKey | Pda;
  destinationTokenAccount: PublicKey | Pda;
};

// Data.
export type MercurialSwapInstructionData = { discriminator: Array<number> };

export type MercurialSwapInstructionDataArgs = {};

export function getMercurialSwapInstructionDataSerializer(): Serializer<
  MercurialSwapInstructionDataArgs,
  MercurialSwapInstructionData
> {
  return mapSerializer<
    MercurialSwapInstructionDataArgs,
    any,
    MercurialSwapInstructionData
  >(
    struct<MercurialSwapInstructionData>(
      [['discriminator', array(u8(), { size: 8 })]],
      { description: 'MercurialSwapInstructionData' }
    ),
    (value) => ({ ...value, discriminator: [2, 5, 77, 173, 197, 0, 7, 157] })
  ) as Serializer<
    MercurialSwapInstructionDataArgs,
    MercurialSwapInstructionData
  >;
}

// Instruction.
export function mercurialSwap(
  context: Pick<Context, 'programs'>,
  input: MercurialSwapInstructionAccounts
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'jupiter',
    'JUP6LkbZbjS1jKKwapdHNy74zcZ3tLUZoi5QNyVTaV4'
  );

  // Accounts.
  const resolvedAccounts = {
    swapProgram: {
      index: 0,
      isWritable: false as boolean,
      value: input.swapProgram ?? null,
    },
    swapState: {
      index: 1,
      isWritable: false as boolean,
      value: input.swapState ?? null,
    },
    tokenProgram: {
      index: 2,
      isWritable: false as boolean,
      value: input.tokenProgram ?? null,
    },
    poolAuthority: {
      index: 3,
      isWritable: false as boolean,
      value: input.poolAuthority ?? null,
    },
    userTransferAuthority: {
      index: 4,
      isWritable: false as boolean,
      value: input.userTransferAuthority ?? null,
    },
    sourceTokenAccount: {
      index: 5,
      isWritable: true as boolean,
      value: input.sourceTokenAccount ?? null,
    },
    destinationTokenAccount: {
      index: 6,
      isWritable: true as boolean,
      value: input.destinationTokenAccount ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Default values.
  if (!resolvedAccounts.tokenProgram.value) {
    resolvedAccounts.tokenProgram.value = context.programs.getPublicKey(
      'splToken',
      'TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA'
    );
    resolvedAccounts.tokenProgram.isWritable = false;
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getMercurialSwapInstructionDataSerializer().serialize({});

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
