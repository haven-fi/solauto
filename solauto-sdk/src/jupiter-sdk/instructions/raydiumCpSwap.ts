/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  array,
  mapSerializer,
  struct,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type RaydiumCpSwapInstructionAccounts = {
  swapProgram: PublicKey | Pda;
  payer?: PublicKey | Pda;
  authority?: PublicKey | Pda;
  ammConfig: PublicKey | Pda;
  poolState: PublicKey | Pda;
  inputTokenAccount: PublicKey | Pda;
  outputTokenAccount: PublicKey | Pda;
  inputVault: PublicKey | Pda;
  outputVault: PublicKey | Pda;
  inputTokenProgram: PublicKey | Pda;
  outputTokenProgram: PublicKey | Pda;
  inputTokenMint: PublicKey | Pda;
  outputTokenMint: PublicKey | Pda;
  observationState: PublicKey | Pda;
};

// Data.
export type RaydiumCpSwapInstructionData = { discriminator: Array<number> };

export type RaydiumCpSwapInstructionDataArgs = {};

export function getRaydiumCpSwapInstructionDataSerializer(): Serializer<
  RaydiumCpSwapInstructionDataArgs,
  RaydiumCpSwapInstructionData
> {
  return mapSerializer<
    RaydiumCpSwapInstructionDataArgs,
    any,
    RaydiumCpSwapInstructionData
  >(
    struct<RaydiumCpSwapInstructionData>(
      [['discriminator', array(u8(), { size: 8 })]],
      { description: 'RaydiumCpSwapInstructionData' }
    ),
    (value) => ({
      ...value,
      discriminator: [54, 234, 83, 141, 52, 191, 46, 144],
    })
  ) as Serializer<
    RaydiumCpSwapInstructionDataArgs,
    RaydiumCpSwapInstructionData
  >;
}

// Instruction.
export function raydiumCpSwap(
  context: Pick<Context, 'identity' | 'payer' | 'programs'>,
  input: RaydiumCpSwapInstructionAccounts
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'jupiter',
    'JUP6LkbZbjS1jKKwapdHNy74zcZ3tLUZoi5QNyVTaV4'
  );

  // Accounts.
  const resolvedAccounts = {
    swapProgram: {
      index: 0,
      isWritable: false as boolean,
      value: input.swapProgram ?? null,
    },
    payer: {
      index: 1,
      isWritable: false as boolean,
      value: input.payer ?? null,
    },
    authority: {
      index: 2,
      isWritable: false as boolean,
      value: input.authority ?? null,
    },
    ammConfig: {
      index: 3,
      isWritable: false as boolean,
      value: input.ammConfig ?? null,
    },
    poolState: {
      index: 4,
      isWritable: true as boolean,
      value: input.poolState ?? null,
    },
    inputTokenAccount: {
      index: 5,
      isWritable: true as boolean,
      value: input.inputTokenAccount ?? null,
    },
    outputTokenAccount: {
      index: 6,
      isWritable: true as boolean,
      value: input.outputTokenAccount ?? null,
    },
    inputVault: {
      index: 7,
      isWritable: true as boolean,
      value: input.inputVault ?? null,
    },
    outputVault: {
      index: 8,
      isWritable: true as boolean,
      value: input.outputVault ?? null,
    },
    inputTokenProgram: {
      index: 9,
      isWritable: false as boolean,
      value: input.inputTokenProgram ?? null,
    },
    outputTokenProgram: {
      index: 10,
      isWritable: false as boolean,
      value: input.outputTokenProgram ?? null,
    },
    inputTokenMint: {
      index: 11,
      isWritable: false as boolean,
      value: input.inputTokenMint ?? null,
    },
    outputTokenMint: {
      index: 12,
      isWritable: false as boolean,
      value: input.outputTokenMint ?? null,
    },
    observationState: {
      index: 13,
      isWritable: true as boolean,
      value: input.observationState ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Default values.
  if (!resolvedAccounts.payer.value) {
    resolvedAccounts.payer.value = context.payer.publicKey;
  }
  if (!resolvedAccounts.authority.value) {
    resolvedAccounts.authority.value = context.identity.publicKey;
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getRaydiumCpSwapInstructionDataSerializer().serialize({});

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
