/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  array,
  mapSerializer,
  struct,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type TokenSwapV2InstructionAccounts = {
  swapProgram: PublicKey | Pda;
  swap: PublicKey | Pda;
  authority?: PublicKey | Pda;
  userTransferAuthority: PublicKey | Pda;
  source: PublicKey | Pda;
  swapSource: PublicKey | Pda;
  swapDestination: PublicKey | Pda;
  destination: PublicKey | Pda;
  poolMint: PublicKey | Pda;
  poolFee: PublicKey | Pda;
  sourceMint: PublicKey | Pda;
  destinationMint: PublicKey | Pda;
  sourceTokenProgram: PublicKey | Pda;
  destinationTokenProgram: PublicKey | Pda;
  poolTokenProgram: PublicKey | Pda;
};

// Data.
export type TokenSwapV2InstructionData = { discriminator: Array<number> };

export type TokenSwapV2InstructionDataArgs = {};

export function getTokenSwapV2InstructionDataSerializer(): Serializer<
  TokenSwapV2InstructionDataArgs,
  TokenSwapV2InstructionData
> {
  return mapSerializer<
    TokenSwapV2InstructionDataArgs,
    any,
    TokenSwapV2InstructionData
  >(
    struct<TokenSwapV2InstructionData>(
      [['discriminator', array(u8(), { size: 8 })]],
      { description: 'TokenSwapV2InstructionData' }
    ),
    (value) => ({
      ...value,
      discriminator: [51, 48, 145, 115, 123, 95, 71, 138],
    })
  ) as Serializer<TokenSwapV2InstructionDataArgs, TokenSwapV2InstructionData>;
}

// Instruction.
export function tokenSwapV2(
  context: Pick<Context, 'identity' | 'programs'>,
  input: TokenSwapV2InstructionAccounts
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'jupiter',
    'JUP6LkbZbjS1jKKwapdHNy74zcZ3tLUZoi5QNyVTaV4'
  );

  // Accounts.
  const resolvedAccounts = {
    swapProgram: {
      index: 0,
      isWritable: false as boolean,
      value: input.swapProgram ?? null,
    },
    swap: { index: 1, isWritable: false as boolean, value: input.swap ?? null },
    authority: {
      index: 2,
      isWritable: false as boolean,
      value: input.authority ?? null,
    },
    userTransferAuthority: {
      index: 3,
      isWritable: false as boolean,
      value: input.userTransferAuthority ?? null,
    },
    source: {
      index: 4,
      isWritable: true as boolean,
      value: input.source ?? null,
    },
    swapSource: {
      index: 5,
      isWritable: true as boolean,
      value: input.swapSource ?? null,
    },
    swapDestination: {
      index: 6,
      isWritable: true as boolean,
      value: input.swapDestination ?? null,
    },
    destination: {
      index: 7,
      isWritable: true as boolean,
      value: input.destination ?? null,
    },
    poolMint: {
      index: 8,
      isWritable: true as boolean,
      value: input.poolMint ?? null,
    },
    poolFee: {
      index: 9,
      isWritable: true as boolean,
      value: input.poolFee ?? null,
    },
    sourceMint: {
      index: 10,
      isWritable: false as boolean,
      value: input.sourceMint ?? null,
    },
    destinationMint: {
      index: 11,
      isWritable: false as boolean,
      value: input.destinationMint ?? null,
    },
    sourceTokenProgram: {
      index: 12,
      isWritable: false as boolean,
      value: input.sourceTokenProgram ?? null,
    },
    destinationTokenProgram: {
      index: 13,
      isWritable: false as boolean,
      value: input.destinationTokenProgram ?? null,
    },
    poolTokenProgram: {
      index: 14,
      isWritable: false as boolean,
      value: input.poolTokenProgram ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Default values.
  if (!resolvedAccounts.authority.value) {
    resolvedAccounts.authority.value = context.identity.publicKey;
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getTokenSwapV2InstructionDataSerializer().serialize({});

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
